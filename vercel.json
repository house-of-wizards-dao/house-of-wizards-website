{
  "buildCommand": "npm run build",
  "installCommand": "pnpm install --frozen-lockfile",
  "functions": {
    "pages/api/**/*.ts": {
      "maxDuration": 10
    }
  },
  "env": {
    "SKIP_ENV_VALIDATION": "false"
  }
}